"use client";

import SectionHeader from "@/components/SectionHeader";

export default function Subscribe() {
  return (
    <div className="h-full w-full">
      <SectionHeader title="구독 설정" />
    </div>
  );
}
